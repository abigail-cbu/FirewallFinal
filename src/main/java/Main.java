/**
 * Abigail Lu
 * CSC 513 - Final
 * Description: Created a Firewall that has the following features:
 * 1. An Access list to keep URLs
 *      a. With a method to add a URL
 *      b. And a method to find a URL within the access list
 * 2. A packet
 *      a. Assume the packet format is a URL Address (16 characters) followed by the Packet Payload (64 characters)
 *      b. With a method to extract the URL address.
 * 3. A Log to keep rejected URL’s.
 *      a. With a method that accepts a URL and stores it in a Log entry.
 *      b. The Log entry should be 16 characters long.
 */
public class Main {
    
}
